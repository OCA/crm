This module extends CRM Project Create to allow selecting a project marked as
template when creating a project from a lead or opportunity.

Only projects with the **Is Template?** option enabled are available in the
project template selector.
